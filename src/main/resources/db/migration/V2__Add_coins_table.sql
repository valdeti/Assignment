CREATE TABLE coins
(
    coin_id    VARCHAR(255) NOT NULL PRIMARY KEY,
    rank       VARCHAR(255) NULL,
    symbol     VARCHAR(255) NULL,
    name       VARCHAR(255) NULL,
    supply     VARCHAR(255) NULL,
    max_supply VARCHAR(255) NULL
);
